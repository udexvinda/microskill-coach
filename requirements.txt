# Streamlit Cloud friendly (prebuilt wheels; avoids source builds)

streamlit>=1.38,<1.41

# Data stack (pandas 2.2 needs numpy >=1.26; keep <2 to avoid edge wheels)
pandas>=2.2,<2.3
numpy>=1.26,<2.0

# Charts (pulls pillow automatically; keep <3.10 to avoid bleeding edge)
matplotlib>=3.8,<3.10
pillow>=10,<13

# PDF text
pypdf>=5,<6

# OpenAI (new SDK)
openai>=1.45,<2

# Light QoL (optional; safe)
requests>=2.31,<3
tqdm>=4.66,<5
